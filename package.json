{
  "name": "exocortex",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:check": "npm run build && test -z \"$(git status --porcelain -- dist)\"",
    "check": "npm run build && node --check scripts/*.mjs scripts/lib/*.mjs dist/core/*.js dist/terminal/*.js dist/runtime/worker/*.js dist/storage/sqlite/*.js src/cli/*.mjs src/diagnostics/*.mjs src/adapters/lark-im/*.mjs",
    "help": "node scripts/help.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
